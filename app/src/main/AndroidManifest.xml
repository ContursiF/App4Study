<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.contursif.app4study">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AdapterActivity"
            android:label="Seconda Activity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".Activity_Fragment_hw" />
        <activity android:name=".AdapterGridActivity" />
        <activity android:name=".AdapterSpinnerActivity" />
        <activity android:name=".FragmentCity"></activity>
    </application>

</manifest>